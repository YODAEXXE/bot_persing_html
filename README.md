# bot_persing_html
This Python CLI tool performs basic web scraping and HTML parsing using requests and BeautifulSoup. It can fetch raw page content, extract titles, find hyperlinks, and detect phone numbers via regex. The interactive menu allows users to choose tasks dynamically, making it a simple yet practical foundation for automation and data extraction.
